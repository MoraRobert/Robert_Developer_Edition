public class PracticeSplit {
    
    public static void practiceSplit(){
        String str = 'abc;def;hij;klm;';
        List<String> lista = new List<String>(str.split(';'));
        //lista = str.split(';');
        //List<String> listOfIds = new List<String>(receivedList.get(0).split(';'));
    }

}